<div class="flex flex-wrap gap-4">
  <% %i[brand dark success neutral].each do |variant| %>
    <%= render Ui::BadgeComponent.new(variant: variant).with_content(variant.to_s.humanize) %>
  <% end %>
</div>
