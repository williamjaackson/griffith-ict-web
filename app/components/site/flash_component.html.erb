<div class="px-6 pt-6">
  <div class="container flex flex-col gap-2">
    <% messages.each do |type, message| %>
      <%= render Ui::AlertComponent.new(variant: variant(type), dismissible: true) do %>
        <%= message %>
      <% end %>
    <% end %>
  </div>
</div>
