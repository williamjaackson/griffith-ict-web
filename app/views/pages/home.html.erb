<div class="flex flex-col items-start gap-8">
  <h1 class="text-2xl font-bold">hello, world!</h1>

  <div class="flex flex-wrap items-start gap-4">
    <%= ui_button("Join our Discord", href: "#", variant: :primary) %>
    <%= ui_button("Learn more", href: "#", variant: :secondary) %>
    <%= ui_button("Join Us", href: "#", variant: :primary, size: :sm) %>
  </div>

  <div class="grid grid-cols-2 gap-4 w-full">
    <%= ui_button("Join our Discord", href: "#", variant: :primary, class: "h-full w-full") %>
    <%= ui_button("Learn more", href: "#", variant: :secondary, class: "h-full w-full") %>
    <%= ui_button("Join Us", href: "#", variant: :primary, size: :sm, class: "h-full w-full") %>
    <%= ui_button("About", href: "#", variant: :secondary, size: :sm, class: "h-full w-full") %>
  </div>
</div>
